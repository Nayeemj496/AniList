<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="https://anilist.co/img/icons/favicon-32x32.png"
    />
    <title>Write Review &#183; AniList</title>
    <link rel="stylesheet" href="/styles/generic.css" />
    <style>
      textarea {
        display: block;
        background-color: #151f2e;
        border: none;
        border-radius: 5px;
        margin: 30px auto;
        padding: 20px;
        color: rgb(159, 173, 189);
        color: #fff;
        font-size: 14px;
        font-family: "Roboto";
      }

      textarea:focus {
        outline: none;
      }

      button {
        display: block;
        margin: 10px auto 10px auto;
        border: none;
        background-color: #3db4f2;
        color: #fff;
        padding: 10px 20px;
        border-radius: 5px;
        transition: opacity 0.2s;
      }

      button:hover {
        opacity: 0.7;
      }
    </style>
  </head>

  <body>
    <%- include("navcontainer.ejs", { isAdmin, userimage, username }) %>

    <form action="/review/manga/editor/<%=id%>" method="POST">
      <textarea
        name="review"
        id="review"
        cols="80"
        rows="40"
        placeholder="Write a review..."
      ></textarea>
      <button type="submit" class="review-submit">Submit</button>
    </form>

    <script src="/scripts/tooltip.js"></script>
  </body>
</html>
